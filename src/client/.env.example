# Base URL of the portal server. In production this is the Cloudflare Tunnel
# hostname (https://checkout-api.neuralnexus.site); http://localhost:8080 in
# local development. Vite inlines this at build time, so a rebuild/redeploy is
# required after changing it (set it in the Vercel project env for production).
VITE_API_BASE_URL=
